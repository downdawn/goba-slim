# ADR 0002：显式 Schema 管理

- 状态：已接受
- 日期：2026-07-13

## 决策

数据库变更以按序 SQL 文件交付，由部署方手动执行。常驻服务仅检查 PostgreSQL、必要表和 Schema 版本，不执行运行时自动迁移。

## 原因

生产变更需要备份、审计、锁和回滚控制；把迁移绑定到每个服务实例会放大并发执行和权限风险。

## 后果

发布说明必须明确 SQL 变化。新环境可用 `goba db init` 初始化空库，已有数据库按版本执行增量 SQL。
