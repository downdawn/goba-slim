version: "2"
run:
  timeout: 5m
linters:
  enable:
    - bodyclose
    - contextcheck
    - errcheck
    - errorlint
    - gosec
    - ineffassign
    - misspell
    - staticcheck
    - unused
  exclusions:
    generated: strict
formatters:
  enable:
    - gofmt
    - goimports
